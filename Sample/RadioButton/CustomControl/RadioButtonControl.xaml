<?xml version="1.0" encoding="UTF-8"?>
<buttons:SfButton xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             x:Class="RadioButton.CustomControl.RadioButtonControl"
                  EnableRippleEffect="False" BackgroundColor="Transparent" 
                  PressedBackgroundColor="Transparent" 
                  HorizontalOptions="Start" IsCheckable="True">
    <buttons:SfButton.Content>
        <Grid x:Name="BaseGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <buttons:SfRadioButton HorizontalOptions="Center" VerticalOptions="Center" x:Name="radioControl" IsChecked="{Binding IsChecked}" Grid.Column="0"/>
            
        </Grid>
    </buttons:SfButton.Content>
</buttons:SfButton>